/**
 * APT Layer: features (vertical slices)
 * -------------------------------------
 * Feature-scoped UI + view-models composed by `pages/`. Public exports only;
 * cross-feature reach into another feature's internals is forbidden.
 *
 * Empty today — pages still own composition. As pages grow, extract their
 * page-specific subtree into `features/<name>/` and re-export here.
 */
export {};
